spring:
  application:
    name: rentdetective
  profiles:
    active: dev

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: io.github.shardkiht.rentdetective.**.entity
  configuration:
    map-underscore-to-camel-case: true
